package com.github.arhor.journey.ui

import androidx.fragment.app.Fragment

/**
 * Legacy fragment placeholder kept only to avoid stale references while the POI details page
 * hosts the GLTF selector directly from Compose.
 */
class ItemsSelectionFragment : Fragment()
